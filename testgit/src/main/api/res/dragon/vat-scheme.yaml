/{vatSchemeId}:
  description: Fetching a VAT scheme
  uriParameters:
    vatSchemeId:
      type: integer
      description: The id of the VAT scheme
      example: 12
  get:
    responses:
      200:
        body:
          application/json:
            schema: !include res/vat-scheme.schema.json
            example: !include res/vat-scheme.example.json
 